namespace Domain.Enums;

public enum CarReportType
{
    ChangeGPS, // Request to change GPS device
    DeactivateCar, // Request to deactivate car from the system
    Other // Any other type of report
}
